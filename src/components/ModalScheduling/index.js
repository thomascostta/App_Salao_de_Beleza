import React from "react";
import { ScrollView, Dimensions, View, Text } from "react-native";
import ModalHeader from "./header";

const ModalScheduling = () => {
  return (
    <ScrollView >
      <View><Text>trste</Text></View>
    </ScrollView>
  );
};

export default ModalScheduling;
